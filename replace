#include <stdio.h>
#include <string.h>
int main()
{
    char st[100];
    printf("enter the string");
    gets(st);
    for(int i=0;i<strlen(st);i++)
    {
        if(st[i]==' ')
        {
            st[i]='-';
        }
    }
    printf("%s",st);
}
